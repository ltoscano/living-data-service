version: '3.8'

services:
  living-data-service:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./living-pdfs:/app/living-pdfs
    env_file:
      - .env
    restart: unless-stopped
